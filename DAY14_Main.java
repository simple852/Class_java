/**
 * - 함수형 인터페이스
 *   람다로 사용할 수 있게 해주는 기능
 *
 *   `
 *   @FunctionalInterface
 *   interface [인터페이스명] {
 *       // 추상 메소드/함수 한개만 작성
 *   }
 *   `
 *
 * - 디폴트 메소드(default method)
 */

public class DAY14_Main {


    public static void main(String[] args) {
int n = 8;
int m = 4
        int[] section = {2,3,6};

        Solution solution = new Solution(n,m,section);
        System.out.println(solution);

    }


        class Solution {
            public int solution(int n, int m, int[] section) {
                int answer = 0;
                int[] wall = new int[n];

                for(int i = 0; i< w)



                return answer;
            }
        }
        )


}
